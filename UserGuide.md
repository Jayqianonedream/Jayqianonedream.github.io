Jump to parent folder of Jayqianonedream.github.io

Then run this command in terminal to render the website:

```shell
docsify serve Jayqianonedream.github.io
```

Then open github desktop to sync the change
